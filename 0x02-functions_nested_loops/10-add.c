#include "holberton.h"

/**
 *add - a function that adds two integers and returns the result
 *@x: First number being used
 *@y: Second number being used
 *Return: Always Zero (success)
 */

int add(int x, int y)
{
	return (x + y);
}
